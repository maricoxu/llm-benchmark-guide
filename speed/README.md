# Speed（速度）维度

> 衡量模型的推理速度、延迟和吞吐量

## 📋 概述

Speed维度关注模型的推理性能，包括：

- ⚡ **延迟（Latency）**: 单次推理的响应时间
- 🚀 **吞吐量（Throughput）**: 单位时间处理的请求数
- 🔧 **优化策略**: 提升推理速度的方法

---

## 📊 评测指标

### 延迟指标

- **首Token延迟（TTFT）**: 从请求到第一个token生成的时间
- **端到端延迟**: 从请求到完整响应的时间
- **不同输入长度延迟**: 不同输入长度下的延迟表现

### 吞吐量指标

- **Tokens/秒（TPS）**: 单位时间生成的token数
- **请求/秒（RPS）**: 单位时间处理的请求数
- **不同批量大小吞吐量**: 不同批量大小下的吞吐量

---

## 🛠️ 评测方法

### 延迟评测

```python
import time

def measure_latency(model, input_text):
    start_time = time.time()
    output = model.generate(input_text)
    end_time = time.time()
    
    latency = end_time - start_time
    return latency, output
```

### 吞吐量评测

```python
def measure_throughput(model, inputs, batch_size):
    start_time = time.time()
    outputs = model.batch_generate(inputs, batch_size)
    end_time = time.time()
    
    throughput = len(inputs) / (end_time - start_time)
    return throughput
```

---

## 💡 优化策略

### 模型优化

- **量化**: INT8、INT4量化
- **剪枝**: 模型剪枝减少参数量
- **蒸馏**: 知识蒸馏到小模型

### 推理优化

- **批处理**: 使用批处理提高吞吐量
- **KV Cache**: 使用KV Cache减少重复计算
- **动态批量**: 动态调整批量大小

---

## 📚 相关资源

- [评测工具](../tools/README.md)
- [使用指南](../guides/README.md)

---

**最后更新**: 2025-11-30

